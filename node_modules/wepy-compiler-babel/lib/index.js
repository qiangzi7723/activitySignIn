'use strict';

exports.__esModule = true;

exports.default = function (content, config) {
    var p = void 0;
    try {
        var code = (0, _babelCore.transform)(content, config).code;
        p = Promise.resolve(code);
    } catch (e) {
        p = Promise.reject(e);
    }
    return p;
};

var _babelCore = require('babel-core');

;